<ContentPage
    x:Class="AutoServiceMobil.Views.AppointmentsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Programări">

    <VerticalStackLayout Padding="20">
        <CollectionView x:Name="AppointmentsList">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="10" Margin="5">
                        <VerticalStackLayout>
                            <Label Text="{Binding Data}" />
                            <Label Text="{Binding Serviciu}" />
                            <Button Text="Detalii" CommandParameter="{Binding Id}" Clicked="OnDetailClicked" />
                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>

</ContentPage>